# PRD：侧边栏选中项去深黑效果

## 背景

用户反馈：选中项的深黑效果去掉。当前激活项样式 = 文字 `--m-text`（#191919 深黑）+ `font-weight: 600` 加粗，看起来比其他项"更黑更重"。要求与普通项完全一致（对齐椒盐：激活项无任何视觉区分，实测文字全 #1E1715 相同）。

## 需求（仅 TabsPage.vue）

1. 删除 `&--active` 的 `font-weight: 600`（深黑加粗效果），其余无选中态样式
2. 保留：图标恒灰、无背景、无按压效果（已定）

## 验收

- [ ] 选中项与普通项视觉完全一致（文字同色同字重）
- [ ] MuMu 真机验证
- [ ] lint / type / build 通过
