# v0.4.5

## 修复

- **检查更新可用性修复**：此前点击“检查更新”在网络失败/403/解析异常时被 `?.let` 静默吞掉，无任何 Toast 表现为“不可用”；现改为显式分支：失败统一提示“检查更新失败，请稍后重试”，已是最新提示“已是最新版本”，有新版提示并跳转 `html_url`。`HttpURLConnection` 补 `User-Agent: Muses/<version> (Android)` 与 `finally disconnect`，避免被 GitHub API 以 403 拒绝及 `checking` 状态卡死。

- **沉浸式播放页黑屏修复**：`af4c2940` 注释声称加 `100vh` 实未加，导致 `html/body height:100%` 在 WebView 初始布局 0 高时，`#player-ui/#background-layer/#lyric-layer` 的 `inset:0` 塌陷为 0 高黑屏。现补全 `frontend/amll-web/src/style.css` 三处 `height: 100vh`（视口相对高度，直接撑满），并执行 `vite build` 重建 `feature/player/src/main/androidAssets/amll` 产物（`index.html` 新 hash `DN92VDL6.css/DicGyz_U.js`），保持硬件加速与 `ready` 握手不变。

## 其他

- 构建产物更新：AMLL 前端重编，APK 内 WebView 资源为最新构建
