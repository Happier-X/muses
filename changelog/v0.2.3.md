# v0.2.3

## 组件与浮层

- **PlayerPage 迁移 HPopup fullscreen**：根改为 `HPopup position="fullscreen"`，启用 `keepAlive` 保活 AMLL 背景，关闭 `swipeClose` 保留自建手势；App 常驻挂载，去掉 `keepPlayerPageMounted` / translate-y 保活路径。
- **QueuePage 迁移 HPopup fullscreen**：替换宿主 `fixed inset-0` 与 App 层 Transition；补 fullscreen 高度链；列表滚动与下滑关闭隔离。
- **删除 ExploreContainer**：清理无引用死代码。

## 界面与体验

- **导航栏固定**：修正滚动归属与高度链，顶栏不再随内容卷走。
- **edge-to-edge 安全区**：Capacitor 8 `SystemBars insetsHandling: 'css'`；采用 `var(--safe-area-inset-*, env(...))` 三级回退。
- **沉浸式进度条**：隐藏 HRange knob，恢复 4px 轨道与沉浸式主题色。

## 依赖升级

- `happier-ui` 升级至 **0.0.8**（精确锁定；含 HPopup `keepAlive` / `swipeClose`，以及 edge-to-edge safe-area 正式接管）

## 说明

- Player / Queue 滚动锁双锁并存：HPopup `useScrollLock` + 宿主 `muses-overlay-open` class 锁。
- Player z-index 1100（`.h-popup--swipe-disabled` 覆盖），Queue 默认 1200，保证队列叠在播放器之上。
