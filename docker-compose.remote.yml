# 使用 Docker Hub 镜像的 docker-compose 配置
# 使用方式: docker-compose -f docker-compose.yml -f docker-compose.remote.yml up -d

services:
  music-api:
    image: ${DOCKERHUB_USERNAME}/muses-backend:latest
    build:  # 保留 build 配置但不使用

  web:
    image: ${DOCKERHUB_USERNAME}/muses-web:latest
