收尾歌词面板 gutter 预留公式修正(LyricsPanel.kt 未提交改动)

背景:工作区中 `feature/player/.../lyric/LyricsPanel.kt` 有一处未提交修改——聚焦行按 lyricFocusScale 缩放时,grow 侧预留 gutter E 的公式从旧的 `((s-1)·(P-16)-8)/s` 修正为 `(s-1)·P/s - 8`(由 `s·(P-8-E) ≤ P` 推导)。已人工核对:注释推导与代码一致,数学正确,且 `coerceAtLeast(0f)` 兜底 s=1 情况;`focusScaleReservePadding` 仅此一处定义、一处使用(1248-1249 行 flipped-aware padding),无其他波及面。

步骤:
1. 编译验证:运行 `./gradlew :feature:player:compileKotlinJvm`(如 target 名不同则按实际调整),确认改动可编译。
2. 视觉抽查(尽力而为):运行 `./gradlew :composeApp:run` 桌面应用,播放带歌词曲目,在不同窗口宽度下观察聚焦行最大缩放时文本边缘恰好贴住面板边、不被裁剪,且缩放动画中换行点不漂移;若环境不便(无歌词数据源等),则依赖编译验证与数学推导,并说明留给用户目视确认。
3. 提交:验证通过后提交该文件,提交信息沿用现有中文风格,例如 `fix(player): 歌词聚焦行缩放gutter预留宽度公式修正`。

不改动其他任何文件。